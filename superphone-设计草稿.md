# superphone 内部设计草稿（v1）

> 状态：讨论收敛版，未落地实现。所有技术结论按"事实纪律"分级标注。
> 适用场景：私域分发（不上架、不推广、手动发安装包），故 AGPL/合规按私域松弛处理，仅保留"修改版源码对用户可得"的省心习惯。

---

## 0. 产品定位与双 SKU

| SKU | 载体 | 核心能力 | 角色 |
|---|---|---|---|
| **软件版** | 普通手机装 APK（尽量 No Root） | 控屏 + 触控 + 实时双向通话音频 | P0 生死线决定能否成立 |
| **硬件版（定制手机）** | 预刷定制 ROM（含虚拟摄像头 HAL） | 软件版全部 + 虚拟摄像头接管 | 高价噱头，按单定制、零库存 |

两条线共用同一套客户端/密钥/传输底座；硬件版只是多一个"摄像头注入"可插拔模块 + 固件层支持。

---

## 1. 分阶段售卖模型（卖点分层 = 开发攻克顺序）

卖点按能力颗粒度从低到高拆，每一层先实现、再开放售卖：

| 阶段 | 卖点 | 对应能力 | 解锁前提（开发完成） |
|---|---|---|---|
| S1 | **控制** | 控屏 / 触控 / 连接 / 配对 | 客户端底座 + RustDesk 裁剪完成 |
| S2 | **语音** | 实时双向通话音频（取下行 + 注上行） | P0 本地 PoC 通过 + P1 电话桥 |
| S3 | **变声** | 固定音色（原声/低沉男声/清晰女声/机器人） | Relay/Edge 端 DSP 落地 |
| S4 | **摄像头** | 虚拟摄像头注入（喂给微信等） | 定制 ROM + HAL 注入实现（可再拆） |

> 原则：**能卖哪层 = 哪层已做通**。S4 到了具体实现时可继续拆（如 camera-basic / camera-hd），拆法在那时定。

---

## 2. 密钥（Key）权限与计量设计

### 2.1 权限位（entitlement bits）

用位掩码，不用固定 4 级——这样任意组合可售，未来拆摄像头也只需加位。

| Bit | 名称 | 含义 |
|---|---|---|
| `BIT_CONTROL` (1) | 控制 | 控屏/触控/连接/配对（基础，默认含） |
| `BIT_VOICE` (2) | 语音 | 双向通话音频 |
| `BIT_VOICECHANGE` (4) | 变声 | 固定音色切换 |
| `BIT_CAMERA` (8) | 摄像头 | 虚拟摄像头注入（预留，硬件版） |
| （预留）(16/32…) | camera-hd 等 | S4 实现时再定义 |

**销售套餐 = 位的便捷组合**（实际鉴权按位，不按套餐名）：
- 控制版 = `CONTROL`
- 语音版 = `CONTROL | VOICE`
- 变声版 = `CONTROL | VOICE | VOICECHANGE`
- 摄像头版 = `CONTROL | VOICE | VOICECHANGE | CAMERA`

### 2.2 Key 结构

```
key = base64url( payload_json || ed25519_signature )
payload_json = {
  "kid":        "唯一 key id",
  "ent":        12,            // entitlement 位掩码
  "exp":        1770000000,    // 时效：过期时间戳（秒）
  "quota":      {"type":"call_minutes","limit":500},  // 次数计量
  "act_limit":  3,             // 激活次数上限（可绑定设备数）
  "iat":        1700000000,    // 签发时间
  "revoked":    false          // 吊销标记（常用吊销列表替代）
}
```

- 服务端用 **ed25519 私钥**签名；客户端内置**公钥**验签（离线可验，契合私域）。
- 任何字段篡改都会导致验签失败 → 拒绝激活。

### 2.3 激活次数限制（重点）

- 首次激活：客户端采集**设备指纹** `fp = HMAC(device_hwid, salt)`（device_hwid 取 AndroidID + 硬件序列哈希，轻量、不依赖强证明）。
- 激活请求带 `{key, fp}` 到 license 服务 → 服务校验签名 + 检查 `act_limit`：
  - 已绑定 `fp` 数 < `act_limit` → 通过，记录 `fp`；
  - 已达上限 → 拒绝新设备（提示"激活次数已用完"）。
- **释放/转移**：客户端发 `release` 移除本机 `fp` → 腾出一个名额，用户可换手机。
- 防滥用：同一 `fp` 重复激活幂等；`fp` 与服务端记录比对，避免一 key 无限克隆。

### 2.4 时效与次数

- **时效（exp）**：7天/月/季/年/永久。过期后功能停用，需续期（重签新 key 或延长 exp）。
- **次数（quota）**：建议语音层默认按 `call_minutes`（通话分钟）计量；控制层可 `none` 或 `session_count`。
  - 客户端本地计数（轻量），周期性向服务上报；服务端做硬上限兜底。
  - 私域可宽松：先本地计 + 周期对账，不必一上来做严格服务端实时计量。

### 2.5 吊销与续期

- **吊销**：服务端维护 `revoked_kids` 集合 + 可下发的吊销列表；客户端周期拉取，命中即停用。
- **续期**：重新签发（延长 exp / 调整 quota / 加位）生成新 key 给用户，旧 key 可加入吊销列表。

### 2.6 防篡改与防伪

- 签名防改；`iat` + 随机 `kid` 防重放。
- 设备指纹轻量即可（私域），不强求 Keystore 证明；如需更严可后续升级 attestation。

---

## 3. 极简 License 服务

职责最小集，可单文件服务或 CLI 预签：

| 能力 | 接口/动作 | 说明 |
|---|---|---|
| 签发 key | 离线 CLI 或管理页 | 填 payload → 签名 → 出 key 字符串 |
| 激活 | `POST /activate {key, fp}` | 校验签名 + 记 fp + 控 act_limit |
| 释放 | `POST /release {key, fp}` | 移除 fp，腾名额 |
| 上报用量 | `POST /usage {key, delta}` | 累加到 quota |
| 吊销列表 | `GET /revocations` | 返回 revoked_kids |
| 吊销/续期 | 管理动作 | 写库 / 重签 |

> 私域可极简：key 离线预签 + 一个只管激活/吊销的小型服务即可，不必上完整计量后台。

---

## 4. 客户端集成点（推断，需读 RustDesk 源码确认落点）

1. **早期拦截**：启动早期挂 license 模块，验签在"连接建立之前"完成，未激活不能用。
2. **功能门控**：每个模块启动前查 `ent` 对应位，无权限则不暴露 UI/不初始化。
3. **Pro 功能**：编译时直接写死开启全部 Pro 级 feature flag（不接官方 license 后端），key 只管我们的 ent/时效/次数。
4. **配额计数**：轻量本地计数器 + 周期上报。
5. **激活 UI**：首次启动输入 key 的引导页。

---

## 5. 渐进式瘦身路线图（L0→L7，影响从小到大，先确认再删）

| 阶段 | 模块 | 决策 | 风险 |
|---|---|---|---|
| L0 | 远程终端、TCP 隧道、RDP/VPN | **删** | 极低 |
| L1 | 文件传输 | **删**（独立通道） | 低 |
| L1 | clipboard 同步 | **删**（不影响系统复制粘贴，A 触控操作 B 本地复制粘贴照常） | 低 |
| L1 | 键盘（远程输入） | **注释暂留**，确认无影响再动 | 低 |
| L2 | 远程看 B 摄像头模块 | **删**（隐私雷区，非核心） | 低 |
| L2 | virtual display（息屏保活） | **保留评估**（服务后台省电） | 低 |
| L3 | 地址簿 + Web 控制台 | **保留**，但迁到自建轻量后端（不依赖原 account） | 低 |
| L4 | 登录/账号 UI + 后端 account | **移除登录**，改 key 鉴权 | 中（key 须先就绪） |
| L5 | SSO / 2FA / LDAP / AD | 随 L4 移除 | 低 |
| L6 | 多平台裁剪 | **谨慎**，先不构建暂不发布平台 | 低 |
| L7 | 屏幕/触控/配对/加密/P2P·Relay/输入事件 | **禁动** | — |

> 顺序红线：**L4 必须排在 key 体系就绪之后**，否则用户装完无法激活。建议节奏：并行做 key 服务 + 先跑 L0→L3 → key 验收 → 再做 L4/L5 → 最后 L6。

---

## 6. 注入框架预留（MediaInjection）

摄像头注入与通话音频上行注入**同构**（都是往 B 系统注入一路媒体流），共用一套框架：

```rust
// 预留接口，软件版 no-op，硬件版接 HAL
trait MediaInjection {
    fn start(&self, kind: MediaKind);   // AudioUplink | Camera
    fn inject(&self, frame: &[u8]);     // PCM 帧 或 视频帧
    fn stop(&self);
}
```

- 握手阶段带 `capabilities` 字段：软件版报 `audio`，硬件版报 `audio+camera`。
- 现在只留接口与能力协商；定制 ROM 到位后填 HAL 实现，不另起炉灶。

---

## 7. P0 本地 PoC 边界（与跨境解耦）

- P0 只测 **B 端本地**能否调通 `isPstnCallAudioInterceptable()` / `getCallDownlinkExtractionAudioRecord()` / `getCallUplinkInjectionAudioTrack()`，取/注 PCM。
- **不涉及 A 端、不涉及服务器、不涉及 P2P/Relay、不引入跨境链接**。
- 记录字段：Android 版本、机型、SoC、SIM 类型、单/双卡、VoLTE/VoWiFi、API 是否存在、AudioRecord/AudioTrack 是否成功、本地 I/O 延迟、稳定性。
- P0 通过 ≠ 跨境通话可用；跨境是 P3 独立战场。

---

## 8. 开发攻克顺序（对照卖点分层）

| 阶段 | 做通什么 | 可售卖 |
|---|---|---|
| A | 客户端底座 + key 系统 + L0→L3 瘦身 | **控制版（S1）** |
| B | P0 音频 PoC + P1 最小电话桥 | **语音版（S2）** |
| C | Relay/Edge 端固定音色 DSP | **变声版（S3）** |
| D | 定制 ROM + 虚拟摄像头 HAL 注入 | **摄像头版（S4，按单定制）** |

> 软件版先过生死线活下来；硬件版是叠在上面的 premium，有人付钱再定制，零库存。

---

## 9. 待定项 / 风险

- [ ] "次数"具体计量哪种（call_minutes / call_count / session_count）——建议语音层默认 call_minutes。
- [ ] 目标机型测试清单（P0 机型矩阵）——规则列字段未给具体机器。
- [ ] 自建轻量后端（key + 设备 + 地址簿）模块草图待补。
- [ ] 降级 SKU：P0 若部分失败，产品如何收缩（仅控拨接挂、不传音频）。
- [ ] device fingerprint 强度：私域轻量即可，后续如需防伪再升级 attestation。
- [ ] 硬件版"虚拟摄像头喂微信"属 dual-use，受众筛选由业务自定边界。

## 10. 演进纪律（递进不推翻原则）

> 核心约束：**每层做通后再递进，且每层落地时必须为下一层预留干净接口缝——绝不图省事硬塞，避免后期加层推翻前期架构。**

- **预留优先**：control / voice / voicechange / camera 在架构层都以"可插拔模块 + 能力协商"存在，即使当期不实现也留 `trait`/接口位（见 §6 MediaInjection）。
- **一层做完美再进下一层**：S1 控制版必须底座稳、key 鉴权稳，才进 S2 语音；不并行赶工导致两层都半成品。
- **权限位向前兼容**：新增能力只加 `ent` 位、不改既有位含义，老 key 结构不破坏。
- **key 结构向后兼容**：新增字段（quota 类型、激活策略）以可选字段追加，旧 key 仍可验签。
- **降级不伤主链**：P0 失败时收缩的是"售卖范围"，不是推翻已做的控制层架构。

---

## 11. 不可越过的红线

> 任何阶段评审都要回看本页。红线来自项目总规则与 [设计草稿 v1](./superphone-设计草稿.md)，不可协商。

### 一、六条不可越过的红线

| # | 红线 | 具体约束 | 违反后果 |
|---|---|---|---|
| 1 | **先验证，再实现** | 不得从"API 存在"直接推导"普通 APK 可用"。ADB / Shizuku / Wireless Debugging / 各 OEM / VoLTE 必须**实测**。结论强制分级为：已确认 · 推断 · 假设 · 猜测 | 基于假设排期，返工 |
| 2 | **P0 生死线优先** | `CallAudioBridge` 未跑通前，不得魔改整个 RustDesk。不自己实现 SIP / GSM / VoLTE / VoIP / 虚拟手机号 | 掉进通信协议深坑 |
| 3 | **音频链路独立** | 电话音频与屏幕流解耦。拥塞时优先级：**音频 > 控制信令 > 屏幕视频**。屏幕卡顿不得连坐音频 | 体验致命，语音版卖不出去 |
| 4 | **变声不进 B 手机** | 固定音色部署在 Relay/Edge，默认原声、用户主动开启。禁止声音克隆、声纹训练、用户上传声音模型 | 法律与伦理风险 |
| 5 | **L4 排在 key 之后** | 移除登录/账号 UI 必须在 key 体系就绪之后，否则用户装完无法激活 | 装了用不了，版本作废 |
| 6 | **许可证与隐私** | RustDesk(AGPL-3.0)、CallVault/ShizuCallRecorder(GPL-3.0) 仅作**架构参考**，优先自研。通话音频默认**不保存、不上传、不训练** | 见 见 §12 许可证与合规 |

### 二、售卖解锁门禁（能卖哪层 = 哪层已做通）

| 阶段 | 解锁 | 前置门禁（全部满足才允许对外售卖） | 对应任务 |
|---|---|---|---|
| S1 | **控制版** | 客户端可编译可安装；控屏 / 触控 / 配对稳定；key 离线验签通过；激活全流程走通；私域分发包就绪 | A1–A8 |
| S2 | **语音版** | **P0 机型矩阵实测通过**（下行可提取 + 上行可注入，至少 1 款机型清晰可辨）；CallAudioBridge 端到端延迟达标；降级路径可用 | B1–B8 |
| S3 | **变声版** | 4 组固定音色在 Edge 落地；开关与降级策略就绪；延迟与算力成本已量化并告知用户 | C1–C7 |
| S4 | **摄像头版** | 定制 ROM 可刷机；虚拟摄像头 HAL 注入目标 App 验证通过；dual-use 受众边界已由业务侧书面确认 | D1–D5 |

> 门禁是**售卖**门槛，不是**开发**门槛。开发可以并行，售卖不行。

### 三、阶段推进纪律（来自设计草稿 §10）

- **一层做完美再进下一层**：S1 底座稳、key 鉴权稳，才进 S2。不并行赶工导致两层都半成品。
- **每层为下一层预留干净接口缝**：control / voice / voicechange / camera 一律以"可插拔模块 + 能力协商"存在（`MediaInjection` trait），即使当期不实现也留接口位。
- **权限位向前兼容**：新增能力只加 `ent` 位，不改既有位含义。
- **降级不伤主链**：P0 失败时收缩的是售卖范围，不是推翻控制层架构。

### 四、事实纪律（贯穿全程）

任何技术结论必须标注来源等级，写进 [事实分级清单](../poc/事实分级清单.md)：

| 等级 | 含义 | 能否作为设计依据 |
|---|---|---|
| 已确认 | 真机实测复现 | 可以 |
| 推断 | 有源码/文档支撑，未实测 | 需先验证 |
| 假设 | 逻辑上说得通，无证据 | 不可，必须先做 PoC |
| 猜测 | 凭经验 | 禁止进入设计 |

> 设计草稿 §4「客户端集成点」整节标注为**推断**——RustDesk 源码落点待 E02 确认，不要在未确认前写死。

---

## 12. 许可证与合规

> 责任人：**E09 安全 · 开源合规专家**（当前状态：待招募）。在 E09 到位前，本文由项目总控代管，**任何第三方代码引入前必须过一遍本清单**。

### 一、第三方许可证清单

| 项目 | 许可证 | 使用方式 | 义务 | 状态 |
|---|---|---|---|---|
| **RustDesk** | **AGPL-3.0** | 二开（fork 建分支） | 修改版源码对用户可得；网络交互亦触发源码提供义务 | 待 E09 审查 |
| **CallVault / ShizuCallRecorder** | GPL-3.0 | **仅架构参考** | 不直接复用代码则无义务 | 待 E09 审查 |
| **MicUp / P2P-PHONE** | MIT | 参考 | 保留版权声明即可 | 待 E09 审查 |

**总原则**：优先参考架构**自研实现**，不要直接复制 GPL/AGPL 代码。这是设计草稿 §9 与项目规则第 21/22/24/25 节的一致要求。

### 二、AGPL-3.0 的实际影响（重点）

本产品为**私域分发**（不上架、不推广、手动发安装包），因此：

- AGPL 第 13 条的"网络交互须提供源码"义务在私域场景下**实际触发面有限**，但仍应保留"修改版源码对用户可得"的习惯——**这是省心，不是可选项**。
- **真正危险的是混仓**：一旦 superphone 的二开代码与其他私有项目进同一个仓库或构建单元，AGPL 的传染性会波及整个衍生作品。

#### 因此立下硬规矩

1. 本仓库**独立存在**，不与任何其他项目共仓、不共用构建产物。
2. 引入任何第三方依赖前，先在本页登记许可证，E09 确认无传染风险后方可使用。
3. 分发安装包时，随附获取源码的方式说明（哪怕只是一句"联系索取"）。

### 三、隐私红线

| 项 | 约束 |
|---|---|
| 通话音频 | **默认不保存、不上传、不训练**。PoC 阶段落本地文件仅用于验证可听，验证完即删 |
| 设备指纹 `fp` | `HMAC(device_hwid, salt)`，轻量不可逆；不强求 Keystore 证明（私域足够） |
| 会话鉴权 | 不能只依赖设备 ID——设备 ID 可伪造，须叠加会话鉴权 |
| 变声模型 | 禁止声音克隆、声纹训练、用户上传声音模型。仅 4 组固定音色 |

### 四、dual-use 边界（硬件版）

虚拟摄像头 HAL 注入微信等 App 属 **dual-use** 能力。设计草稿 §9 明确：

> 硬件版"虚拟摄像头喂微信"属 dual-use，**受众筛选由业务自定边界**。

任务 **D5「dual-use 商务边界与受众筛选」**（E01 主责，P3）必须在 D3（HAL 实现）**之前**完成书面边界确认，不得先做技术再想边界。

### 五、待办

- [ ] E09 到位后重审本页，出具正式《第三方许可证清单与合规说明》
- [ ] 出具《隐私审计清单（音频不落盘）》
- [ ] 出具《威胁模型与鉴权方案》
- [ ] 明确 key 防伪的 attestation 升级路径（设计草稿 §2.6）

---

## 13. 事实分级清单

> 这是全项目**最重要的一张表**。
> 红线 1 要求：不得从"API 存在"推导"普通 APK 可用"。所有技术结论必须落到这里，并标注等级。
> 负责人：E03（P0 相关）· E01（总控维护）· 任务 X3

### 等级定义

| 等级 | 定义 | 能否作为设计依据 |
|---|---|---|
| **已确认** | 真机实测复现，有记录可追溯 | 可以 |
| **推断** | 有源码 / 官方文档支撑，但未实测 | 需先验证 |
| **假设** | 逻辑上说得通，无证据 | 不可——必须先做 PoC |
| **猜测** | 凭经验直觉 | 禁止进入设计 |

### 当前清单

| # | 结论 | 等级 | 依据 | 影响 | 验证任务 |
|---|---|---|---|---|---|
| 1 | Android 提供 `isPstnCallAudioInterceptable()` 等三个通话音频 API | 推断 | 官方文档存在，未经真机实测 | 决定软件版能否成立 | B2 |
| 2 | 普通 APK（No Root）可成功调用上述 API | **假设** | 无任何实测证据 | **生死线** | B3 / B4 / B5 |
| 3 | ADB / Wireless Debugging / Shizuku 可作为 No Root 替代路径 | 假设 | 社区经验 | 决定是否需要 Root | B5 |
| 4 | RustDesk 客户端集成点位置（设计草稿 §4） | 推断 | 未读源码确认 | 影响 L0→L3 瘦身排期 | A1 |
| 5 | L0→L3 瘦身模块可安全删除（远程终端/TCP 隧道/RDP/VPN/文件传输/clipboard） | 推断 | 设计草稿分析 | 影响 A 阶段工作量 | A1 |
| 6 | virtual display 可用于息屏保活 | 推断 | 设计草稿标注"保留评估" | 影响后台存活 | A1 |
| 7 | ed25519 离线验签方案可满足私域防篡改需求 | 已确认（设计层面） | 成熟密码学方案，非技术风险 | — | A2 |
| 8 | 位掩码 `ent` 权限模型可支持未来拆分层级 | 已确认（设计层面） | 位运算，无技术不确定性 | — | A2 |
| 9 | 通话音频可在 Relay/Edge 端做固定音色 DSP | 推断 | 现有 TTS/音色转换方案存在 | 影响 S3 成本 | C5 |
| 10 | 虚拟摄像头 HAL 可注入微信等 App | 假设 | dual-use 能力，无验证 | 影响 S4 是否可做 | D3 |
| 11 | P2P 优先、失败降级 Relay 的架构可行 | 推断 | RustDesk 已实现同类架构 | — | C2 |

### 填写规则

1. 新结论产生时**立即**入表，不要攒着。
2. 等级只降不升——从"推断"变"已确认"必须附实测记录（机型、日期、方法）。
3. 任何"假设"或"猜测"级别的结论**不得**写入设计文档作为依据，只能在本文档里挂着等待验证。
4. 阶段评审时逐条过，凡影响当阶段决策的"假设"必须先验证后开工。

### 与红线 1 的关系

> 不得从"API 存在"直接推导"普通 APK 可用"；结论分级为已确认 · 推断 · 假设 · 猜测。

清单第 1 条（API 存在）和第 2 条（能用）之间的差距，正是 P0 要测量的距离。**这两条不能混为一谈。**

---

## 14. 当前最高风险

#### R1 · P0 专家未到位（最高）

| 项 | 内容 |
|---|---|
| 风险 | 8 项 P0 生死线任务全部集中在 B 阶段，其中 6 项主责是 **E03**（Telephony 音频），2 项是 **E04**（CallAudioBridge）。**两人均为"待招募 · 全职核心"** |
| 影响 | E03/E04 每延迟 1 个月到位，S2 语音版售卖时间整体后移 1 个月。A 阶段只解锁 S1（控制版），卖不上价 |
| 应对 | 1）招聘优先级高于一切开发工作；2）**B1 机型矩阵先自行筹备**（无依赖，今天可做）；3）A 阶段与招聘并行，不阻塞 |

#### R2 · P0 技术可行性未验证

| 项 | 内容 |
|---|---|
| 风险 | "普通 APK No Root 可提取注入通话音频"目前是**假设**，无任何实测证据 |
| 影响 | 若失败，S2/S3/S4 全部不成立，只能卖 S1 |
| 应对 | 严格走闸门一（B1→B2→B3/B4→B5）。**在出结论前不铺开团队**。A 阶段照样推进，保证失败也有产品可卖 |

#### R3 · B 阶段串行依赖链过长

| 项 | 内容 |
|---|---|
| 风险 | B1→B2→B3/B4→B5→B6/B7→B8 严格串行，合计 53~80 人日，且无法靠加人压缩 |
| 影响 | 时间不可压缩，只能靠尽早启动 |
| 应对 | B1 立即启动；B3 与 B4 可并行（同依赖 B2）；B6 与 B7 可并行（同依赖 B3/B4） |

#### R4 · AGPL 混仓污染

| 项 | 内容 |
|---|---|
| 风险 | RustDesk 为 AGPL-3.0，若与其他私有项目共仓或共用构建产物，传染性波及整个衍生作品 |
| 影响 | 法律层面不可逆 |
| 应对 | **本仓库已独立**。新增第三方依赖前先登记 [许可证与合规](../docs/许可证与合规.md) |

#### R5 · 合规专家未到位导致返工

| 项 | 内容 |
|---|---|
| 风险 | E09（安全合规）待招募，但 X1（许可证审查）、X2（隐私审计）是 P1/P2，若开工后再改代价高 |
| 影响 | 许可证与隐私设计返工 |
| 应对 | 已提前出 [许可证与合规](../docs/许可证与合规.md) 代管版；E09 到位后重审 |
